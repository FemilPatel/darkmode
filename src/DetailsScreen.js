import React, { PureComponent } from 'react'
import { Text, View } from 'react-native'

export default class DetailsScreen extends PureComponent {
    render() {
        return (
            <View >
                <Text style={{color:'#f1404b'}}> textInComponent </Text>
            </View>
        )
    }
}
